@model IEnumerable<RestaurantWebApp.Models.MenuItem>

@{
    ViewBag.Title = "Menu Items";
}

<h2>Menu Items</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<h3>Appetizers</h3>
<table class="table">
    <tr>
        <th style="width: 40%">
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th style="width: 40%">
            @Html.DisplayNameFor(model => model.Price)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        if (item.Category == RestaurantWebApp.Models.Category.Appetizers)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Price)
                </td>
                <td>
                    @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
                    @Html.ActionLink("Details", "Details", new { id = item.Id }) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.Id })
                </td>
            </tr>
        }
    }
</table>


<h3>Food</h3>
<table class="table">
    <tr>
        <th style="width: 40%">
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th style="width: 40%">
            @Html.DisplayNameFor(model => model.Price)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        if (item.Category == RestaurantWebApp.Models.Category.Food)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Price)
                </td>
                <td>
                    @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
                    @Html.ActionLink("Details", "Details", new { id = item.Id }) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.Id })
                </td>
            </tr>
        }
    }
</table>


<h3>Drinks</h3>
<table class="table">
    <tr>
        <th style="width: 40%">
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th style="width: 40%">
            @Html.DisplayNameFor(model => model.Price)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        if (item.Category == RestaurantWebApp.Models.Category.Drinks)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Price)
                </td>
                <td>
                    @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
                    @Html.ActionLink("Details", "Details", new { id = item.Id }) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.Id })
                </td>
            </tr>
        }
    }
</table>
